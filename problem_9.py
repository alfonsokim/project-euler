
# ============================================================
def solve():
    """
    """
    for a in range(1, 1000):
        for b in range(a, 1000):
            c = 1000 - a - b
            if sum([a, b, c]) == 1000 and (a*a) + (b*b) == (c*c):
                return str(a * b * c)


# ============================================================
if __name__ == '__main__':
    """
    """
    print solve()
